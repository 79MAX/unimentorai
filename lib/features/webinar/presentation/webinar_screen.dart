import 'package:flutter/material.dart';

class WebinarScreen extends StatelessWidget {
  const WebinarScreen({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
      body: Center(
        child: Text('Webinars bientôt disponibles'),
      ),
    );
}




