import 'package:flutter/material.dart';

class CertificatPlaceholderScreen extends StatelessWidget {
  const CertificatPlaceholderScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
      appBar: AppBar(title: const Text('Certificat')),
      body: const Center(child: Text('Écran Certificat à implémenter')),
    );
} 




