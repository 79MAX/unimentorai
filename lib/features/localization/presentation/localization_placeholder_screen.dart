import 'package:flutter/material.dart';

class LocalizationPlaceholderScreen extends StatelessWidget {
  const LocalizationPlaceholderScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
      appBar: AppBar(title: const Text('Localisation')),
      body: const Center(child: Text('Écran Localisation à implémenter')),
    );
} 




